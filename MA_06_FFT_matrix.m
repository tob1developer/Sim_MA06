%--------------------------------------------------------------------------
%------------------------- MA_06_FFT_matrix  ------------------------------
%--------------------------------------------------------------------------

function [W] = MA_06_FFT_matrix(N)
W = zeros(N);
for m =1:N
    for i= 1:N
        W(m,i) = exp(-j*2*pi/N*(m-1)*(i-1));
    end
end
W = 1/sqrt(N)*W;